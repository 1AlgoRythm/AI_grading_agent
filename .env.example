# P3 uses SQLite automatically for local Streamlit runs. Docker Compose sets
# this to its PostgreSQL service. Override it for an external database.
DATABASE_URL=sqlite:///p3_demo.db

# BYOK model settings reserved for the shared provider integration.
MODEL_PROVIDER=
MODEL_NAME=
MODEL_API_KEY=

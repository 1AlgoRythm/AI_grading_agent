# Commits

Do not add a `Co-Authored-By: Claude` trailer to commit messages in this repo. All commits should be authored solely as the repo owner (git user.name/user.email already configured locally).
